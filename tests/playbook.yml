---
- hosts: all
  any_errors_fatal: yes
  pre_tasks:
    - name: Include vars
      include_vars: vars.yml
    - name: Install firewalld package
      yum:
        name: '{{ item }}'
        state: present
      loop:
        - firewalld
  roles:
    - ansible-role-haproxy
